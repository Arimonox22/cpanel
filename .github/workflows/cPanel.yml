# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: cPanel
'on':
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: |
             npm i express -save
             npm i  child_process
             chmod 755 ./panel/server.js
             node ./panel/server.js
